{
  "name": "relution-sdk",
  "version": "0.0.1",
  "description": "Relution Software Development Kit for TypeScript and JavaScript",
  "main": "index.js",
  "scripts": {
    "precommit": "npm run tslint",
    "tslint": "tslint src/**/*.ts",
    "test": "npm run build && mocha lib/**/*.spec.js",
    "build": "tsc -p .",
    "api": "typedoc --out public/docs src --module commonjs --target ES5 --name Relution-SDK --exclude **/*.spec.ts",
    "watch": "tsc -p . -w",
    "postinstall": "typings install"
  },
  "author": "Thomas Beckmann",
  "license": "Apache License",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "sinon": "^1.16.0",
    "tslint": "^3.8.1",
    "typedoc": "^0.3.12",
    "typescript": "^1.8.10"
  }
}
